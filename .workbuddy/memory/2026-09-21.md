---
## 2026-09-21 真AI接入 + 双轨制 + 时间戳迁移 + T+1订单引擎 + 飞书通知 + 数据清洗

### 1. 真 AI 接入（MIMO Pro 2.5）
- API Key：用户提供 `tp-cg83vouwhbmrd8f6gcwtpobz7xb5ws6nf0c4t9mawcediyyq`
- Base URL：`https://token-plan-cn.xiaomimimo.com/v1`（tp- 开头用这个）
- Model：`mimo-v2.5-pro`，兼容 OpenAI 格式
- 封装：`backend/ai-advisor.js`，导出 `callMIMO()` 和 `analyzeFund()`
- 测试成功：易方达蓝筹精选混合 → `{decision: "HOLD", confidence: "中", reason: "..."}`

### 2. 双轨制（规则/AI 可切换）
- 后端 `/api/ai/analyze` 加 `mode` 参数（rule/ai）
- 默认走 AI，失败自动降级到规则引擎
- 前端 App.vue 右上角加全局切换按钮
- 日志明确体现走了哪条通道

### 3. 数据库时间戳迁移
- transactions.transaction_date 从 DATETIME 改成 INTEGER（毫秒时间戳）
- 后端加统一工具 `fmtDT(ts)` 和 `fmtDate(ts)`，注入到路由 ctx
- 前端 `formatDate` 兼容时间戳数字作为兜底
- 旧数据已迁移

### 4. T+1 订单引擎（AI 自动交易走 order-engine）
- 之前 AI 自动买入绕过 order-engine，直接写 transactions + holdings → 非交易时间也能立即落账
- 改成走 `order-engine.createOrder()` 生成 SUBMITTED 订单，等 T+1 盘后确认
- 交易时间硬校验：`isTradingTime()`（工作日 9:30-15:00 北京时间），非交易时间只分析不交易

### 5. 数据清洗
- 删除违规交易：aggressive 9/19 20:00 卖出 161725 + 买入 000001（非交易时间）
- 从 transactions 重算 holdings
- 清洗后数据：
  - aggressive: 161725（37847份）+ 005267（7097份）
  - default: 000001（6164份）+ 005827（6706份）
- 数据库已备份

### 6. 飞书通知优化
- Webhook：`https://open.feishu.cn/open-apis/bot/v2/hook/488d7ca1-b75a-41ca-92ff-0d2a2ea11aa7`
- 排版优化：基金代码+名称+净值+涨跌幅+信心度+建议金额+原因
- 加开始时间/结束时间/耗时（自动格式化秒/分/时）
- 所有 AI 操作都推送

### 7. 分析间隔调整
- 从 30 分钟改成 60 分钟（整点触发）
- 非交易时间自动停掉，重排到下一交易日 9:30

### 8. Bug 修复
- 收益明细从 2002 年开始：transaction_date 时间戳用 `String().slice(0,10)` 取到的是数字不是日期 → 加 `tsToDateStr()` 转换
- 观察池 fund_name 为空：LEFT JOIN funds 改成 LEFT JOIN fund_universe
- AI 分析时间 500：`lastBuy.md.slice(0,10)` 在整数上报错 → 时间戳转日期字符串比较
- 数据库路径错误：相对路径改成 `require('path').join(__dirname, 'fund_simulator.db')` 绝对路径

### 9. 文档
- 新增 `docs/USER_GUIDE.md`（用户功能说明）
- 更新 `docs/SYSTEM_KNOWLEDGE.md`（0921 改动摘要）
- 代码规范 `docs/CODING_STANDARDS.md`

### 10. Git
- 仓库：https://github.com/clearPeople13/fund-simulator.git
- 所有改动已 commit 到本地
- push 偶发超时，重试即可
